<UserControl x:Class="EloksalPro.View.LaboratoryView"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" 
             xmlns:viewModel="clr-namespace:EloksalPro.ViewModels" 
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" >
    <UserControl.DataContext>
        <viewModel:LaboratoryAnalysisViewModel/>
    </UserControl.DataContext>
    <TabControl x:Name="tabControl" >
        <TabItem Header="Лабораторные анализы">
            <Border>
                <Border.Background>
                    <LinearGradientBrush StartPoint="1,0" EndPoint="1,2.9">
                        <GradientStop Color="{StaticResource primaryBackColor1}" Offset="1"/>
                        <GradientStop Color="{StaticResource primaryBackColor2}" Offset="0"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="500"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="60"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="10"/>
                    </Grid.RowDefinitions>
                    <Button Grid.Row="0" Grid.Column="0" x:Name="btnNewCustomer"  Margin="10"
						Command="{Binding ShowNewAnalysisViewCommand}"
						Content="Добавить новый анализ" HorizontalAlignment="Left"  
						Style="{StaticResource controlButtonLightGray}" />
                    <TextBox  x:Name="txtId" Grid.Row="0" Grid.Column="1" Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}" Visibility="Hidden" />
                    <TextBlock Grid.Row="2" Text="Список лабораторных анализов:" FontSize="14" Margin="5" Background="Transparent" FontWeight="Medium" FontFamily="Montserrat" Foreground="White" />
                    <dxg:GridControl  Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" x:Name="gridControl" EnableSmartColumnsGeneration="True" ItemsSource="{Binding ListAnalysis, UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" Margin="5" >
                        <dxg:GridControl.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Открыть данные лабораторного анализа" Command="{Binding ShowAnalysisViewCommand}" />
                                <MenuItem Header="Обновить данные" Command="{Binding ShowRefreshDataCommand}"/>
                            </ContextMenu>
                        </dxg:GridControl.ContextMenu>
                        <dxg:GridColumn FieldName="Id" Header="Id" Visible="False"/>
                        <dxg:GridColumn FieldName="NumberAnalysis" Header="Номер анализа" />
                        <dxg:GridColumn FieldName="DateLaboratoryAnalysis" Header="Дата" />
                        <dxg:GridColumn FieldName="Emloyee" Header="ФИО" />
                        <dxg:GridColumn FieldName="Месяц" UnboundType="Integer" UnboundExpression="GetMonth([DateLaboratoryAnalysis])" SortOrder="Ascending" GroupIndex="0"/>
                        <dxg:GridColumn FieldName="Год" UnboundType="Integer" UnboundExpression="GetYear([DateLaboratoryAnalysis])" SortOrder="Descending" GroupIndex="0"/>
                        <dxg:GridControl.View>
                            <dxg:TableView x:Name="tableView" ShowTotalSummary="True" FontSize="12" AllowEditing="False" AutoWidth="True" NewItemRowPosition="Bottom" FontFamily="Times New Roman"  >
                                <dxg:TableView.ColumnHeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                                    </DataTemplate>
                                </dxg:TableView.ColumnHeaderTemplate>
                                <dxg:TableView.RowStyle>
                                    <Style>
                                        <Setter Property="TextElement.Foreground" Value="Black"/>
                                    </Style>
                                </dxg:TableView.RowStyle>
                            </dxg:TableView>
                        </dxg:GridControl.View>
                    </dxg:GridControl>
                </Grid >
            </Border>
        </TabItem>
    </TabControl>
</UserControl>
