<UserControl x:Class="EloksalPro.View.EmployeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" 
             xmlns:viewModel="clr-namespace:EloksalPro.ViewModels" 
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" >
    <UserControl.DataContext>
        <viewModel:EmployeeViewModel/>
    </UserControl.DataContext>
    <TabControl x:Name="tabControl" >
        <TabItem Header="Информация о сотрудниках" >
            <Border>
                <Border.Background>
                    <LinearGradientBrush StartPoint="1,0" EndPoint="1,2.9">
                        <GradientStop Color="{StaticResource primaryBackColor1}" Offset="1"/>
                        <GradientStop Color="{StaticResource primaryBackColor2}" Offset="0"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="500"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="60"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="10"/>
                    </Grid.RowDefinitions>
                    <Button Grid.Row="0" Grid.Column="0" x:Name="btnNewCustomer"  Margin="10"
						Command="{Binding ShowNewEmployeeCommand}"
						Content="Добавить сотрудника" HorizontalAlignment="Left"  
						Style="{StaticResource controlButtonLightGray}" />
                    <TextBox  x:Name="txtEmployee" Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}"  Grid.Row="0" Grid.Column="1" Visibility="Hidden"/>
                    <TextBlock Grid.Row="2" Text="Список сотрудников:" FontSize="14" Margin="5" Background="Transparent" FontWeight="Medium" FontFamily="Montserrat" Foreground="White" />
                    <dxg:GridControl  Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" x:Name="gridControlEmployee" EnableSmartColumnsGeneration="True" ItemsSource="{Binding UserList}" Margin="5" >
                        <dxg:GridControl.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Открыть карточку сотрудника" Command="{Binding ShowEmployeeCommand}" />
                                <MenuItem Header="Обновить данные" Command="{Binding ShowRefreshEmployeeCommand }"/>
                            </ContextMenu>
                        </dxg:GridControl.ContextMenu>
                        <dxg:GridColumn FieldName="Id" Header="Id" Visible="False"/>
                        <dxg:GridColumn FieldName="Login" Header="Логин" />
                        <dxg:GridColumn FieldName="Password" Header="Пароль" Visible="False" />
                        <dxg:GridColumn FieldName="Name" Header="ФИО" />
                        <dxg:GridColumn FieldName="Department" Header="Отдел" />
                        <dxg:GridColumn FieldName="Role" Header="Роль" />
                        <dxg:GridColumn FieldName="PhoneNumber" Header="Номер телефона" />
                        <dxg:GridColumn FieldName="Annotation" Header="Примечание"/>
                        <dxg:GridControl.View>
                            <dxg:TableView x:Name="tableViewEmployee" ShowTotalSummary="True" FontSize="12" AllowEditing="False" AutoWidth="True" NewItemRowPosition="Bottom" FontFamily="Times New Roman"  >
                                <dxg:TableView.ColumnHeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                                    </DataTemplate>
                                </dxg:TableView.ColumnHeaderTemplate>
                                <dxg:TableView.RowStyle>
                                    <Style>
                                        <Setter Property="TextElement.Foreground" Value="Black"/>
                                    </Style>
                                </dxg:TableView.RowStyle>
                            </dxg:TableView>
                        </dxg:GridControl.View>
                    </dxg:GridControl>
                </Grid >
            </Border>
        </TabItem>
    </TabControl>
</UserControl>