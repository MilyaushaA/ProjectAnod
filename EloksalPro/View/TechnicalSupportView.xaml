<UserControl x:Class="EloksalPro.View.TechnicalSupportView"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EloksalPro.View"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" 
             xmlns:viewModel="clr-namespace:EloksalPro.ViewModels">
    <UserControl.DataContext>
        <viewModel:TechnicalSupportViewModel/>
    </UserControl.DataContext>
    <TabControl x:Name="tabControl"  >
        <TabItem Header="Техническая поддержка" >
            <Border>
                <Border.Background>
                    <LinearGradientBrush StartPoint="1,0" EndPoint="1,2.9">
                        <GradientStop Color="{StaticResource primaryBackColor1}" Offset="1"/>
                        <GradientStop Color="{StaticResource primaryBackColor2}" Offset="0"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="60"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="10"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Grid.ColumnSpan="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="300"/>
                            <ColumnDefinition Width="300"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="30"/>
                        </Grid.ColumnDefinitions>
                        <Button  Grid.Column="0" x:Name="btnNewProfile" 
                            Command="{Binding OpenNewTechnicalSupportCommand}"
                            Content="Создать заявку"
                            Style="{StaticResource controlButtonLightGray}"  >
                        </Button>
                        <TextBox  Grid.Column="3" Name="txtId" Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}"  Style="{StaticResource controlText}" />
                    </Grid>
                    <TextBlock Grid.Row="2" Text="Список заявок:" FontSize="14" Margin="5"
                               Background="Transparent" FontWeight="Medium" FontFamily="Montserrat" Foreground="White" Grid.ColumnSpan="2"/>
                    <dxg:GridControl Name="gridControl"  Grid.Row="3" EnableSmartColumnsGeneration="True" ItemsSource="{Binding List,UpdateSourceTrigger=PropertyChanged}" Margin="5" AutoExpandAllGroups="False">
                        <dxg:GridControl.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Открыть карту профиля" Command="{Binding OpenTechnicalSupportCommand}" />
                                <MenuItem Header="Обновить данные" Command="{Binding RefreshDataCommand}"/>
                            </ContextMenu>
                        </dxg:GridControl.ContextMenu>
                        <dxg:GridControl.Columns>
                            <dxg:GridColumn FieldName="Id" Header="Номер заявки" Visible="False"/>
                            <dxg:GridColumn FieldName="Number" Header="Номер заявки"  Width="80"/>
                            <dxg:GridColumn FieldName="DateApp" Header="Дата заявки" Width="80"/>
                            <dxg:GridColumn FieldName="ApplicationText" Header="Заявка" Width="400" />
                            <dxg:GridColumn FieldName="DatePreliminaryReadiness" Header="Дата исполнения" Width="80"/>
                            <dxg:GridColumn FieldName="DateReadiness" Header="Дата готовности" Width="80" />
                            <dxg:GridColumn FieldName="Customer" Header="Заявку создал" Width="200"/>
                        </dxg:GridControl.Columns>
                        <dxg:GridControl.View>
                            <dxg:TableView Name="tableView" ShowTotalSummary="True" FontSize="14" AllowEditing="False" AutoWidth="True" NewItemRowPosition="Bottom" FontFamily="Times New Roman"  >
                                <dxg:TableView.ColumnHeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                                    </DataTemplate>
                                </dxg:TableView.ColumnHeaderTemplate>
                                <dxg:TableView.RowStyle>
                                    <Style>
                                        <Setter Property="TextElement.Foreground" Value="Black"/>
                                    </Style>
                                </dxg:TableView.RowStyle>
                            </dxg:TableView>
                        </dxg:GridControl.View>
                    </dxg:GridControl>
                    
                </Grid>
            </Border>
        </TabItem>
    </TabControl>
</UserControl>

